from typing import Any, Dict

from bucketvision import Resolution

configs: Dict[str, Any] = {
    'camera_res': Resolution(320, 240),
    'output_res': Resolution(320, 240),
    'crop_top': 0,
    'crop_bot': 0.5,
    'brightness': 3
}
